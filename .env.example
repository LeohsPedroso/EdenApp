# Troque pela URL PÚBLICA do backend (não localhost — dentro do celular,
# "localhost" é o próprio celular, não o seu servidor).
# Precisa ser https:// — Android bloqueia chamadas http:// por padrão.
VITE_API_BASE_URL=https://api.edenmc.com.br
